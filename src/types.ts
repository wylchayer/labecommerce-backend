export type TUsers = {
  id: string;
  firstName: string;
  email: string;
  password: string;
  createdAt: string;
};

export type TProducts = {
  id: string;
  firstName: string;
  price: number;
  description: string;
  imageUrl: string;
};
